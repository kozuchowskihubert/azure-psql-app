# 🎹 Techno Studio - Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    TECHNO STUDIO CLI                            │
│                  (techno_studio.py)                             │
│                                                                 │
│  Commands: create | midi | template | automate | list          │
└────────────┬───────────────────┬───────────────┬────────────────┘
             │                   │               │
             ▼                   ▼               ▼
    ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
    │  MIDI          │  │  Template      │  │  VST           │
    │  Generator     │  │  Generator     │  │  Automation    │
    └────────────────┘  └────────────────┘  └────────────────┘
             │                   │               │
             ▼                   ▼               ▼
    ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
    │  6 MIDI Files  │  │  .als Template │  │  Loads VST3    │
    │  (.mid)        │  │  (.als + .xml) │  │  Instruments   │
    └────────────────┘  └────────────────┘  └────────────────┘
             │                   │               │
             └───────────────────┴───────────────┘
                              │
                              ▼
                    ┌─────────────────────┐
                    │   ABLETON LIVE 12   │
                    │   Complete Project  │
                    └─────────────────────┘
```

---

## Module Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                     src/ (Source Code)                          │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  generators/                                              │ │
│  │  ├── midi_generator.py                                    │ │
│  │  │   └── DeepTechnoMIDIGenerator                          │ │
│  │  │       ├── generate_deep_kick()                         │ │
│  │  │       ├── generate_rolling_sub()                       │ │
│  │  │       ├── generate_offbeat_hat()                       │ │
│  │  │       ├── generate_ghost_snares()                      │ │
│  │  │       ├── generate_atmospheric_pad()                   │ │
│  │  │       └── generate_rhythmic_stabs()                    │ │
│  │  │                                                         │ │
│  │  └── template_generator.py                                │ │
│  │      └── AbletonTemplateGenerator                         │ │
│  │          ├── generate_deep_techno_template()              │ │
│  │          └── save_template()                              │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  automation/                                              │ │
│  │  └── vst_automation.py                                    │ │
│  │      ├── AbletonAutomation                                │ │
│  │      │   ├── find_ableton_process()                       │ │
│  │      │   ├── activate_window()                            │ │
│  │      │   ├── setup_view()                                 │ │
│  │      │   ├── navigate_to_track()                          │ │
│  │      │   └── load_plugin()                                │ │
│  │      │                                                     │ │
│  │      └── DeepTechnoSetup                                  │ │
│  │          └── run()                                        │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
USER INPUT
    │
    ├── Genre: deep
    ├── BPM: 124
    └── Bars: 136
    │
    ▼
┌─────────────────────────┐
│  TechnoStudio CLI       │
│  (Main Coordinator)     │
└────────┬────────────────┘
         │
         ├─────────────────────────────┐
         │                             │
         ▼                             ▼
┌────────────────────┐        ┌────────────────────┐
│ DeepTechnoMIDI     │        │ AbletonTemplate    │
│ Generator          │        │ Generator          │
└────────┬───────────┘        └────────┬───────────┘
         │                             │
         │ For each pattern:           │
         │ ├─ Create MIDI file         │ ├─ Create XML structure
         │ ├─ Add tempo                │ ├─ Add master track
         │ ├─ Add notes                │ ├─ Add MIDI tracks
         │ └─ Save .mid                │ ├─ Add return tracks
         │                             │ └─ Compress to .als
         ▼                             ▼
┌────────────────────┐        ┌────────────────────┐
│ output/MIDI-Files/ │        │ output/Projects/   │
│ └─ Deep/           │        │ └─ .als template   │
│    ├─ 01-*.mid     │        └────────────────────┘
│    ├─ 02-*.mid     │
│    ├─ 03-*.mid     │                │
│    ├─ 04-*.mid     │                │
│    ├─ 05-*.mid     │                │
│    └─ 06-*.mid     │                │
└────────────────────┘                │
         │                             │
         └─────────────┬───────────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  ABLETON LIVE   │
              │  (User opens)   │
              └────────┬────────┘
                       │
                       ▼
              ┌─────────────────┐
              │ AbletonAutomation│
              │ (Optional VST    │
              │  loading)        │
              └────────┬─────────┘
                       │
                       ├─ Find process
                       ├─ Activate window
                       ├─ Setup view
                       ├─ Navigate tracks
                       └─ Load VST3 plugins
                       │
                       ▼
              ┌─────────────────┐
              │  READY TO       │
              │  PRODUCE!       │
              └─────────────────┘
```

---

## Command Flow

```
./techno_studio.py create --genre deep --bpm 124
                     │
                     ├─ Parse arguments
                     │
                     ▼
            ┌────────────────┐
            │ TechnoStudio   │
            │ .full_workflow()│
            └────────┬───────┘
                     │
                     ├─ Step 1: Generate MIDI
                     │         ↓
                     │  ┌──────────────────┐
                     │  │ .generate_midi() │
                     │  └──────────────────┘
                     │
                     ├─ Step 2: Generate Template
                     │         ↓
                     │  ┌──────────────────────┐
                     │  │ .generate_template() │
                     │  └──────────────────────┘
                     │
                     ├─ Step 3: Print Instructions
                     │         ↓
                     │  "Open template in Ableton"
                     │
                     ├─ Step 4: (if --autoload)
                     │         ↓
                     │  ┌──────────────────┐
                     │  │ .automate_vst()  │
                     │  └──────────────────┘
                     │
                     └─ Step 5: Print Summary
                              ↓
                       ┌────────────────┐
                       │ WORKFLOW       │
                       │ COMPLETE       │
                       └────────────────┘
```

---

## File Organization

```
Ableton-Automation/
│
├── 🎹 ENTRY POINT
│   └── techno_studio.py          ← Run this!
│
├── 📦 SOURCE CODE
│   └── src/
│       ├── generators/
│       │   ├── midi_generator.py      ← MIDI creation logic
│       │   └── template_generator.py  ← Template creation logic
│       └── automation/
│           └── vst_automation.py      ← VST loading logic
│
├── 📂 OUTPUT
│   └── output/
│       ├── MIDI-Files/
│       │   └── Deep/
│       │       ├── 01-Deep-Kick.mid
│       │       ├── 02-Rolling-Sub.mid
│       │       ├── 03-Off-Beat-Hat.mid
│       │       ├── 04-Ghost-Snares.mid
│       │       ├── 05-Atmospheric-Pad.mid
│       │       └── 06-Rhythmic-Stabs.mid
│       │
│       └── Projects/
│           ├── Deep-Techno-Template.als   ← Open this in Ableton
│           └── Deep-Techno-Template.xml   ← Debug version
│
├── 📚 DOCUMENTATION
│   ├── README.md                   ← Main docs
│   ├── START-HERE.md               ← Quick start
│   ├── PROJECT-ORGANIZATION.md     ← What was done
│   └── docs/
│       ├── USAGE-GUIDE.md          ← Detailed guide
│       └── DEEP-TECHNO-README.md   ← Genre docs
│
└── 🗄️ ARCHIVE (after cleanup.sh)
    └── archive/
        ├── old-scripts/
        └── old-docs/
```

---

## Class Relationships

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                     TechnoStudio                           │
│                   (Main Controller)                        │
│                                                            │
│  + print_banner()                                          │
│  + generate_midi(genre, bpm, bars)                         │
│  + generate_template(name, tempo)                          │
│  + automate_vst()                                          │
│  + full_workflow(genre, bpm, bars, autoload)               │
│  + list_outputs()                                          │
│  + print_summary()                                         │
│                                                            │
└──────────┬──────────────────┬──────────────────┬───────────┘
           │                  │                  │
           │ uses             │ uses             │ uses
           │                  │                  │
           ▼                  ▼                  ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ DeepTechnoMIDI  │  │ AbletonTemplate │  │ AbletonAutomation│
│ Generator       │  │ Generator       │  │                 │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│ + bpm           │  │ + tempo         │  │ + process_name  │
│ + bars          │  │ + output_dir    │  │ + PLUGINS       │
│ + output_dir    │  │                 │  │ + TIMING        │
│                 │  │                 │  │                 │
│ Methods:        │  │ Methods:        │  │ Methods:        │
│ + generate_*()  │  │ + generate()    │  │ + find_process()│
│ + generate_all()│  │ + save_template()│  │ + activate()    │
│                 │  │                 │  │ + load_plugin() │
└─────────────────┘  └─────────────────┘  └─────────────────┘
                                                    │
                                                    │ uses
                                                    ▼
                                          ┌─────────────────┐
                                          │ DeepTechnoSetup │
                                          ├─────────────────┤
                                          │ + TRACK_MAP     │
                                          │                 │
                                          │ Methods:        │
                                          │ + run()         │
                                          └─────────────────┘
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│                  APPLICATION LAYER                      │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ CLI Interface│  │  Generators  │  │  Automation  │  │
│  │  (argparse)  │  │   (classes)  │  │  (pyautogui) │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                    LIBRARY LAYER                        │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   midiutil   │  │     gzip     │  │  subprocess  │  │
│  │ (MIDI files) │  │ (.als files) │  │ (AppleScript)│  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
                          │
┌─────────────────────────────────────────────────────────┐
│                   SYSTEM LAYER                          │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Python 3   │  │    macOS     │  │ Ableton Live │  │
│  │  Interpreter │  │ (GUI Auto)   │  │  12 Suite    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## Execution Timeline

```
Time   Action
─────  ──────────────────────────────────────────────────

0s     User runs: ./techno_studio.py create --genre deep

0.1s   ┌─ CLI parses arguments
       └─ TechnoStudio initialized

0.2s   ┌─ DeepTechnoMIDIGenerator created
       ├─ Generating 01-Deep-Kick.mid
       ├─ Generating 02-Rolling-Sub.mid
       ├─ Generating 03-Off-Beat-Hat.mid
       ├─ Generating 04-Ghost-Snares.mid
       ├─ Generating 05-Atmospheric-Pad.mid
       └─ Generating 06-Rhythmic-Stabs.mid

1.5s   └─ All MIDI files saved

1.6s   ┌─ AbletonTemplateGenerator created
       ├─ Building XML structure
       ├─ Adding tracks
       ├─ Adding returns
       ├─ Compressing to .als
       └─ Saving files

2.0s   └─ Template saved

2.1s   ┌─ Printing summary
       └─ Instructions displayed

DONE   ✅ User opens Ableton and drags MIDI files
```

---

**This architecture provides:**
- ✅ Clear separation of concerns
- ✅ Modular, reusable components
- ✅ Easy to test and maintain
- ✅ Extensible for new features
- ✅ Professional code organization
